<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class ="mb-3">
    <%= form.label 'Nombre:', style: "display: block" %>
    <%= form.text_field :name, class:"form-label" %>
  </div>

  <div class ="dropdown mb-3">
    <%= form.label 'Cliente', style: "display: block" %>
    <%= form.select :client_id, options_for_select(@clients), class:"dropdown btn btn-primary" %>
  </div>

  <div >
    <%= form.submit "Guardar", class:"btn btn-outline-primary mb-3" %>
  </div>
<% end %>
